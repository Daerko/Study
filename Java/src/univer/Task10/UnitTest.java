package univer.Task10;

import org.junit.jupiter.api.RepeatedTest;
import org.junit.jupiter.api.Test;

import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.List;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class UnitTest {

    @Test
    void getMaxIncome() {
        // List<User> list = new ArrayList<>();
       //new User("sasd", "B", new BigDecimal(1000), new BigDecimal(1), new BigDecimal(3), new BigDecimal(1), new BigDecimal(1), new BigDecimal(2));
       // new User("swasdd", "B", new BigDecimal(1000), new BigDecimal(1), new BigDecimal(3), new BigDecimal(1), new BigDecimal(1), new BigDecimal(2));
       // double maxSalary = list.getMaxIncome;
       // assertEquals(15000, maxSalary);
    }
}
